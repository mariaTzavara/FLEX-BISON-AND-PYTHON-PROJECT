CREATE TABLE students (
    id INT,
    name VARCHAR(50),
    grade FLOAT
);

CREATE TABLE courses (
    course_id INT,
    title VARCHAR(60)
);

SELECT id, name, grade
FROM students;

SELECT course_id, title
FROM courses;
