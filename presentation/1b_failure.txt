CREATE TABLE students (
    id INT,
    name VARCHAR(40),
    grade FLOAT
);

SELECT id, name
FROM students
WHERE grade >= 5.0;

CREATE TABLE courses (
    course_id INT,
    title VARCHAR(60)
;

SELECT course_id, title
FROM courses;
